version: "3.9"

services:
  db:
    image: postgres:15
    container_name: lift_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: lift_system
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    container_name: lift_backend
    depends_on:
      - db
    ports:
      - "8000:8000"
    env_file:
      - .env